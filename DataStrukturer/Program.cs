using System;

namespace DataStrukturer
{
    class Program
    {
        static void Main(string[] args)
        {
            ADTWorker worker = new ADTWorker();
            //worker.DoArray();         // Meget programering 
            //worker.DoList();          // sætter alle elementer ind
            //worker.DoDictionary();    // Bruger nøgler til index (slags primær nøgle)
            //worker.DoLinkedList();    // Bruger pointer dvs lettere at indsætte og tage ud Men mere administration
            //worker.DoQueue();         // FIFO
            //worker.DoStack();         // FILO
            //worker.DoHashSet();       // Equal


            Console.ReadLine();
        }
    }
}
